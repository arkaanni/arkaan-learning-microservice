micronaut:
    application:
        name: subject-service

datasource:
    subject:
        jdbcUrl: jdbc:mysql://${db_host}:${db_port}/subject
        username: ${db_user}
        password: ${db_password}
        dialect: mysql
        driverClassName: com.mysql.cj.jdbc.Driver